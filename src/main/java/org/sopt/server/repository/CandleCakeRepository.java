package org.sopt.server.repository;

import org.sopt.server.domain.CandleCake;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CandleCakeRepository extends JpaRepository<CandleCake, Long> {
}
